apiVersion: v1
kind: Service
metadata:
  name: reportingservice
spec:
  selector:
    app: reportingservice
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
