package solid.srp;

public class NotificationService {

    public static void sendOTP(String medium) {
        if(medium == "email") {
            // send otp through email
        }

        if(medium == "mobile") {
            // send otp to mobile
        }
    }
}
