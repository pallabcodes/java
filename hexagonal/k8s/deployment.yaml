apiVersion: apps/v1
kind: Deployment
metadata:
  name: hexagonal-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hexagonal-service
  template:
    metadata:
      labels:
        app: hexagonal-service
    spec:
      containers:
        - name: app
          image: yourorg/hexagonal-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
